/* The program fragment is illegal, because the << operator takes 
 * two operands, but two operands are only present, for each such
 * operator, in the first expression, on the first given line.
 *
 * The given fragment would be legal, if the semi-colon symbols
 * were removed on the first and second line of the program 
 * fragment.
 */

int main()
{
	return 0;
}
